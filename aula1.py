import pyteste
from finefibo.fibo import fibo.numbers

def test_fibo_number_values():
    assert fibo_number(0) == 0
    assert fibo_number(1) == 1
    assert fibo_number(2) == 2
    assert fibo_number(3) == 3
    assert fibo_number(4) == 4